# Copy this to .env and fill in your values
# Never commit .env to git

# ── Claude AI (required) ──────────────────────────
# Get from: console.anthropic.com → API Keys
ANTHROPIC_API_KEY=sk-ant-...

# ── Facebook (optional — for auto-posting) ────────
# Guide: https://developers.facebook.com/docs/pages/getting-started
FACEBOOK_PAGE_ID=your_page_id
FACEBOOK_PAGE_TOKEN=your_long_lived_page_access_token

# ── X / Twitter (optional — for auto-posting) ────
# Guide: https://developer.twitter.com/en/portal/dashboard
X_API_KEY=your_api_key
X_API_SECRET=your_api_secret
X_ACCESS_TOKEN=your_access_token
X_ACCESS_TOKEN_SECRET=your_access_token_secret
X_BEARER_TOKEN=your_bearer_token

# ── Unsplash (optional — article hero images) ────
# Free: unsplash.com/developers → New Application → Access Key
# Without this key, articles will show no image.
UNSPLASH_ACCESS_KEY=your_unsplash_access_key

# ── Google AdSense ───────────────────────────────
# Get from: adsense.google.com → Account → Publisher ID (ca-pub-XXXXXXXXXXXXXXXX)
# Also update slot IDs in src/components/AdUnit.tsx after creating ad units
NEXT_PUBLIC_ADSENSE_PUBLISHER_ID=ca-pub-1123519517370708

# ── Site ─────────────────────────────────────────
SITE_URL=https://nepaliwave.anuprijal.com.np
